Stream Audio Input
2Deck DJ and Playlist mode switch
Actions for github to autobuild the project and publish (might need to add versioning and add tags for that) for linux and windows (if possible)
update Readme


Issues:
Log does not auto scroll to last entry